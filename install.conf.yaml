- clean: ['~']

- default:
    link:
        create: true
        relink: true
        force: true

- shell:
    - ./bin/gen-xmobarrc > ~/.xmobarrc

- link:
    ~/.config/dunst: dunst
    ~/.config/mimeapps.list: mimeapps.list
    ~/.bash_aliases: aliases
    ~/.config/nvim: vim
    ~/.config/termite: termite
    ~/.config/fontconfig/conf.d/75-noto-color-emoji.conf: 75-noto-color-emoji.conf
    ~/.dircolors: dircolors
    ~/.gitconfig: gitconfig
    ~/.gitignore_global: gitignore_global
    ~/.floorc.json: floorc.json
    ~/.git_templates: git_templates
    ~/.ssh/config: ssh_config
    ~/.tmux.conf: tmux.conf
    ~/.vim: vim
    ~/.Xresources: Xresources
    ~/.xinitrc: xinitrc
    ~/.Xmodmap: Xmodmap
    ~/.xmonad: xmonad
    ~/.weechat: weechat
    ~/bin: bin
    ~/.zshrc: zshrc
    /etc/modprobe.d/nobeep.conf: nobeep.conf
    /etc/systemd/logind.conf.d/no-suspend-on-lidswitch.conf: no-suspend-on-lidswitch.conf
    /etc/systemd/logind.conf.d/suspend-on-powerbutton.conf: suspend-on-powerbutton.conf
    /etc/vconsole.conf: vconsole.conf
    /etc/X11/xorg.conf.d/20-intel.conf: 20-intel.conf
    /etc/X11/xorg.conf.d/20-trackpoint.conf: 20-trackpoint.conf

#- shell:
#  -
#    - git update-submodules
#    - Install/update submodules
